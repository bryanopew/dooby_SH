import React from 'react';
import {
    FlatList,
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
    Button
  } from 'react-native';

  const Menu =() => {
      return(
        <Text>식단</Text>
      )
  }
  
  const Home = () => {
      return(
        <Menu />
      )
  }

  export default Home;