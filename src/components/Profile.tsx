import React from 'react';
import {
    FlatList,
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
    Button
  } from 'react-native';

  const Menu =() => {
      return(
        <Text>프로필탭</Text>
      )
  }
  
  const Profile = () => {
      return(
        <Menu />
      )
  }

  export default Profile;